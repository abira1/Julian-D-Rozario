# Local Development Environment Configuration
# Julian D'Rozario Portfolio - Local MySQL Setup

# Database Configuration (Local Development - SQLite)
# For quick local development, we'll use SQLite instead of MySQL
DATABASE_TYPE=sqlite
DATABASE_PATH=./julian_portfolio.db

# MySQL settings (for production deployment later)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_local_mysql_password
MYSQL_DATABASE=julian_portfolio

# JWT Authentication
JWT_SECRET=julian-drozario-local-dev-secret-key-2025
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=24

# Google OAuth Configuration (optional for local dev)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Authorized Admin Emails
AUTHORIZED_ADMIN_EMAILS=juliandrozario@gmail.com,admin@localhost

# Environment
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate Limiting (memory-based)
RATE_LIMIT_PER_MINUTE=100